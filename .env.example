# Admin CMS login (/admin). Required in production — pick strong values.
ADMIN_USERNAME=
ADMIN_PASSWORD=
# Long random string used to sign the admin session cookie (e.g. `openssl rand -hex 32`).
ADMIN_SESSION_SECRET=
